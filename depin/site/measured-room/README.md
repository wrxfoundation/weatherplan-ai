# Measured Room — 집 하나의 공기 시뮬레이션 (v0.4, 2026-09-02)

서우 제안: "실내 공간에 디바이스로 공기 측정하는 것을 시뮬레이션하는 VR 월드". 단일 HTML, 외부 라이브러리 없음 —
거실·침실·부엌 세 공간, 상황(요리·창문·밤)마다 바뀌는 **실사 장면(사진 + 5초 앰비언트 영상 루프, 기기는 장면 속에 렌더링)** 위에
자체 WebGL 셰이더로 먼지(PM2.5)·김(조리)·뿌연 막과 광원 산란(CO₂·연기)·필름 그레인을 얹고, 기기 화면은 2D 캔버스를 원근으로 합성한다.
PM2.5·CO₂가 변하면서 5분마다 기록 하나가 쌓인다.

- 용도: 10/3 부스 화면(터치), 랜딩 데모, 키노트 클립 소재. 캡션은 항상 "개념 시뮬레이션 · concept render".
- 규칙: 화면에 공기질 등급 라벨 없음(곡선·숫자만) · 토큰·코인 아이콘 없음 · "보상은 테스트 중" 고정 문구 · 실물 사진 대체 아님 · 공식 공개 전 `noindex`.

## v0.4 — 무엇이 바뀌었나 (서우 피드백: "폴리곤 깨짐 · 광원·가구 퀄리티 · CO₂ 입자 실감 · 엔진급")

| | v0.3 | v0.4 |
|---|---|---|
| 기기 | GLB 모형을 브라우저 3D 로 사진 위에 띄움 → 폴리곤·조명 불일치가 보임 | **기기를 장면 사진 자체에 렌더링**(Nano Banana Pro 합성, 같은 구도로 상태 변형) → 조명·그림자·재질이 방과 한 장에서 맞는다 |
| 장면 | 정지 사진 10장 | 사진 10장 + **같은 구도의 5초 영상 루프 10편**(Kling 3.0 pro, 첫 프레임 = 사진, 커튼·김·조명만 움직이고 기기 고정). 사진이 먼저 뜨고 영상이 준비되면 그 위로 페이드 |
| 기기 화면 | 3D 평면 텍스처(각도 어긋남) | 2D 캔버스(수치·곡선·상태)를 사진 속 화면 네 모서리에 **`matrix3d` 원근**으로 정확히 얹음 + 은은한 발광 |
| 먼지·CO₂ | three.js 점 스프라이트, 균일한 막 | **자체 WebGL**: 깊이별 보케(가까운 입자는 크고 흐리게, 먼 입자는 작고 선명하게), **광원(창 빛살·램프) 안에서만 반짝임**(틴들 효과), 창 열면 창에서 안으로 흐르는 바람, 조리 중엔 팬 위 상승 기류. CO₂·연기는 fbm 노이즈 안개(위쪽이 짙게) + 광원 주변 산란 블룸 |
| 김 | 흰 점 | 노이즈 텍스처 퍼프가 팬에서 올라와 퍼지며 사라짐, 창 열면 후드로 빨려감(영상 속 김과 겹쳐 부피감) |
| 마감 | — | 필름 그레인, 상시 비네트, 마우스·기울기 시차(장면은 조금, 가까운 먼지는 더) |
| 의존성 | three.js + GLTFLoader(CDN) | 없음(Google Fonts 만). 부스 현장에서 CDN 이 막혀도 효과는 동작 |

"언리얼·유니티급"에 대한 답: 브라우저 실시간 3D 로 그 화질을 내려면 방·가구·기기의 고품질 3D 자산과 조명 베이크가 필요해 지금 파이프라인으론 어렵다.
대신 **사전 렌더(실사 사진·영상)를 바탕으로 두고, 데이터에 반응하는 부분(먼지·안개·김·화면)만 실시간 셰이더로 얹는** 하이브리드가 같은 예산에서 화질이 가장 높다.
참고한 접근: canvasui.dev 의 Clouds(노이즈 안개)·Blaze(스파크·연기), threeui.com 의 Particle Drift·광선 배경 — 코드는 가져오지 않고 같은 기법(fbm 노이즈·소프트 파티클·광원 마스크)을 자체 셰이더로 썼다.

## 공간과 상황

| 공간 | 버튼 | 사진 상태 | 공기 |
|---|---|---|---|
| 거실 | 창문 열기/닫기 · 밤 지나기 | 저녁(기본) / 창 열림 / 밤 | 저녁엔 사람 숨으로 CO₂ ↑, 창 열면 ↓. 부엌 연기가 조금 넘어온다 |
| 침실 | 창문 열기/닫기 · 밤 지나기 | 저녁 / 밤 / 아침(창 열림) | 밤(23~07시) 닫힌 방에서 CO₂ 가장 높음, 아침에 창 열면 ↓ |
| 부엌 | 요리 15분 · 창문 열기/닫기 · 밤 지나기 | 대기 / 조리 중 / 환기(창 열림) / 밤 | 조리 15분에 PM2.5 급상승(약 280까지), 창 열면 빠짐 |

세 공간이 같은 시계를 쓴다(1초 = 1분, 밤엔 1초 = 12분, 06:30 까지). 기록은 공간마다 5분에 하나, 아래 띠는 지금 공간, 숫자는 지금 공간 / 집 전체.

장면을 바로 열려면 해시: `#space=kitchen&cook=1` · `#space=bedroom&night=1` · `#space=living&window=1` (부스 QR·링크용).

## Vercel 배포 (정적 사이트, 빌드 없음)

```
cd measured-room
vercel --prod
```
- 첫 실행에서 프로젝트 이름은 `wellbian-measured-room`, 프레임워크는 Other, 빌드 명령·출력 디렉터리는 비워 둔다(루트의 index.html 을 그대로 서빙).
- **자산 파일을 따로 넣지 않아도 된다.** 사진 10장(≈1MB)과 영상 루프 10편(≈7MB, 보이는 것만 순서대로 받음)은 힉스필드 CDN(CORS `*`, 1년 캐시)에서 바로 불러온다. 데이터 절약 모드·모션 줄이기 설정에서는 영상 없이 사진만.
- 오래 쓸 배포(공식 공개·부스 오프라인)라면 `sh fetch-assets.sh` 로 `assets/` 에 받아 두면 그쪽을 먼저 쓴다(자세한 것은 `assets/README.md`).
- 외부 의존: Google Fonts 뿐(막히면 시스템 글꼴로 대체). 장면 사진·영상은 힉스필드 CDN(CORS `*`)에서 바로 불러오고, `sh fetch-assets.sh` 로 `assets/` 에 받아 두면 그쪽을 먼저 쓴다.

## 자산 좌표를 손보고 싶을 때

`index.html` 의 `SPACES` 에 공간마다 사진 안 비율 좌표가 있다: `screen`(기기 화면 네 모서리 — 왼쪽 위부터 시계 방향; 사진에서 어두운 화면 패널을 자동 검출한 값),
`pan`·`hood`(김이 나오는 곳과 빨려 들어가는 곳), `window`(바람 시작점), `focus`(모바일에서 장면을 어느 쪽 중심으로 자를지).
`SCENES` 에는 장면마다 광원이 있다: `kind: "beam"` 은 창(`at` 에서 `to` 방향 빛살), `"lamp"` 는 램프(`at` 둘레 `r`), `on` 은 세기, `col` 은 색.
사진을 새로 만들면 이 숫자만 맞추면 된다. 영상은 사진과 같은 구도여야 한다(첫 프레임 = 사진).

## 다음 단계

- 15초 시네마틱: 부엌 조리 → 거실 저녁 → 침실 밤 순서로 화면 녹화(영상 루프가 있으니 캡처만으로 클립이 된다).
- 부스용 자동 시연 모드(입력 없으면 공간·상황을 순서대로 돌기), 기기 화면에 기록 카운트 표시.
- 실사 촬영본으로 교체할 때는 같은 구도 사진 + 5초 정지 촬영 루프만 있으면 코드 수정 없이 `SCENES` 의 파일명만 바꾼다.
